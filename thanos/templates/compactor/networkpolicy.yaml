{{- if and .Values.compactor.enabled .Values.compactor.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "thanos.fullname" . }}-compactor
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "thanos.labels" . | nindent 4 }}
    app.kubernetes.io/component: compactor
spec:
  podSelector:
    matchLabels:
      {{- include "thanos.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: compactor
  policyTypes:
    - Ingress
    - Egress
  {{- if .Values.compactor.networkPolicy.allowExternalEgress }}
  egress:
    - {}
  {{- else }}
  egress:
    # Allow DNS resolution
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # Allow HTTPS egress to GCS (port 443)
    - ports:
        - port: 443
          protocol: TCP
    {{- if .Values.compactor.networkPolicy.extraEgress }}
    {{- toYaml .Values.compactor.networkPolicy.extraEgress | nindent 4 }}
    {{- end }}
  {{- end }}
  ingress:
    - ports:
        - port: 10902
      {{- if not .Values.compactor.networkPolicy.allowExternal }}
      from:
        - podSelector:
            matchLabels:
              {{- include "thanos.selectorLabels" . | nindent 14 }}
        {{- if .Values.compactor.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- toYaml .Values.compactor.networkPolicy.ingressNSMatchLabels | nindent 14 }}
          {{- if .Values.compactor.networkPolicy.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.compactor.networkPolicy.ingressNSPodMatchLabels | nindent 14 }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- if .Values.compactor.networkPolicy.extraIngress }}
    {{- toYaml .Values.compactor.networkPolicy.extraIngress | nindent 4 }}
    {{- end }}
{{- end }}
