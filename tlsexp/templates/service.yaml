apiVersion: v1
kind: Service
metadata:
  name: {{ include "tlsexp.fullname" . }}
  labels:
    {{- include "tlsexp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "tlsexp.selectorLabels" . | nindent 4 }}
